# Bug Audit — 2026-06-13

> **FIX STATUS (2026-06-13, applied to working tree, not yet committed):**
> - **Fixed:** all HIGH **#1–#10**; MEDIUM **#13,#14,#15,#16,#17,#18,#19,#21,#22,#23,#25**; LOW **#28,#30,#31,#32,#33,#36,#37**. (`app.py` + `mtv_processor.py` py_compile clean; inline JS `node --check` clean.)
> - **Deferred (riskier / behavioral / low value):**
>   - #11 broad subprocess `timeout=` — only added to the slide pdftoppm call; the dub/burn/slide ffmpeg calls still lack timeouts (broad sweep).
>   - #12 `jobs`/`mtv_jobs`/`uploads` retention/eviction — needs a TTL design.
>   - #20 concurrent-dub same-job race — needs an in-flight lock.
>   - #24 token-in-query logging — left as-is (intentional login-by-link UX).
>   - #26 subtitle/dub SSE auto-reconnect — behavioral, larger change.
>   - #27 Gemini Files API leak (move delete into finally).
>   - #29 regex escaped-quote truncation (rare last-resort path).
>   - #34 MTV beat-snapping slot drops (risky montage-timing change).
>   - #35 detect_gemini_tts_speed wrong model (value effectively unused for gemini).
> - **Note:** #7 fix honors a changed voice on re-burn but still does not thread `tts_model` through `burn_dub_task` (route doesn't pass it).


巡检结果（多 agent 巡检 + 人工逐条核对源码）。HIGH 级别均已对照源码逐行确认。
所有问题都在 `main` 上**早已存在**，与未提交的 Whisper idle-unload 改动无关
（该改动本身经核实是正确的：`_whisper_busy` 在 `get_whisper_model()` 之前于锁内自增，
unloader 在 `busy>0` 时跳过，`gc.collect()/empty_cache()` 在锁外执行）。

建议从 HIGH 里的「静默失败 / 卡死」一组先修：#1 #2 #3 #5 #6（可合成一个提交）。
注意 #5/#6/#16 同源（live dict 迭代竞争 → 用 `dict(...)` 快照/加锁），#3/#8 同源（音频引擎分支不全）。

---

## HIGH（已逐行核实）

1. **`max_chars=0` → 死循环 / CPU 卡死** — `app.py:251-264`(`split_long_subtitle`) & `225-232`(`wrap_subtitle_text`)
   `cap=0` 时 `cut` 恒为 0，`rest` 永不缩短 → daemon 线程内死循环占满一核。
   max_chars 从 burn 路由的 client JSON 未校验流入。**修:** API 边界 `max(1,int(...))` + `cut=max(1,cut)`。

2. **Slide-dub 进度线程泄漏 + 状态永卡 `running`(pdftoppm 失败时)** — `app.py:1934-1949`,`2160`
   `stop_monitor.set()` 只在 `subprocess.run(check=True)` 成功后执行；失败则 `_monitor` 永远循环、
   每秒用默认 `status="running"` 覆盖掉外层设的 `error`。SSE 永远收不到终态 → 前端卡死、任务不被清理。
   **修:** pdftoppm 调用用 `try/finally: stop_monitor.set()`。

3. **Slide-dub 选 OpenAI/MeloTTS/Fish 声音 → 全静音视频，却报成功** — `app.py:2059-2072`
   分发只处理 `gemini`/`edge`，但 `VOICE_ROUTE`(1360-1374) 还有 `openai`/`melo`/`fish`，
   且下拉框 OpenAI 声音排在最前。**修:** 补齐其它引擎调用，或对不支持的引擎明确报错。

4. **`burn_dub_task` 复用旧 `_dubbed.mp4`，忽略新选的声音和已编辑字幕** — `app.py:1784-1810`
   只在 `not dubbed_path.exists()` 时重配音，从不比对 `voice.txt`。换声音后 burn 仍用旧声音。
   编辑字幕后同理（复用旧 `_dubbed_subtitles.json`）。**修:** 比对已存声音标记，不同则重配音。

5. **`api_status`/`api_job_info` 边迭代 live `job` dict 边被 worker 插键 → `RuntimeError` 中断 SSE** — `app.py:2267`,`2281`
   worker 线程在 job 生命周期里插入 `duration`/`model_warning`/`title`/`subtitles`/`dubbed_path` 等新键，
   CPython 抛 "dictionary changed size during iteration"。（MTV 路径有锁，这里没有。）**修:** `data = dict(job)` 原子快照。

6. **`_purge_old_tasks` 无锁迭代任务字典 → 一次 `RuntimeError` 永久杀死清理线程** — `app.py:2592`
   对 `dub_tasks`/`burn_tasks`/`slide_tasks` 的列表推导与 worker 插键竞争，无 try/except，
   线程一死任务从此无限增长（恰好违背该线程的目的）。**修:** `list(store.items())` + 循环体包 `try/except`。

7. **MTV：多个 PDF 互相覆盖页图** — `mtv_processor.py:187/201` + `356`
   `pdf_to_images` 对每个 PDF 都往同一 `pdf_dir` 写 `pdf_{i:04d}.png`，第二个 PDF 覆盖第一个，
   而 `visual_items` 仍指向这些路径 → 静默渲染错内容。**修:** 每源加文件名前缀，或全局页计数。

8. **`concat_wav_files -c copy` 采样率不一致致配音损坏** — `app.py:1326` + `tts_fish:1247-1257`/`tts_openai:1269`
   `-c copy` 不重采样、按首段头部走。Fish(44.1k)/OpenAI 原样写入与 24k 静音交织 → 变速变调、音画错位。
   **修:** 所有 TTS 输出统一规整到 24k/mono/s16，或 concat 改重编码。

9. **并发转写共用同一 ctranslate2 模型且 `model.transcribe()` 无锁** — `app.py:604-660`
   `_whisper_lock` 只护 `_whisper_busy` 计数；实际 transcribe + 惰性 generator 消费在锁外。
   两个并发字幕任务驱动同一非重入模型 → 转写串扰/原生崩溃。**修:** 用专门互斥量包住整个 transcribe+消费，或字幕任务全局信号量=1。

10. **历史记录标题/文件名未转义 → 存储型 DOM XSS** — `templates/index.html:2043`
    `<div class="history-name">${h.filename}</div>` 原样插值（2479 的 MTV 代码正确用了 `escHtml()`）。
    `h.filename` 来自 AI 生成标题或上传文件名，存在 localStorage 每次加载触发。**修:** `${escHtml(h.filename)}`。

---

## MEDIUM

11. **dub/burn/slide 流水线的 ffmpeg/pdftoppm `subprocess.run` 无 `timeout=`**(`app.py` 多处)
    编码器卡住（如 RTX3080 "GPU is lost" 掉卡）→ worker 永久阻塞，任务卡 `running`。mtv_processor 与 `detect_silence_regions` 已传 timeout，这些没有。
12. **`jobs`/`mtv_jobs` 字典与 `uploads/` 文件从不清理** — `app.py:2586-2596` 只清 dub/burn/slide 任务 → 内存+磁盘无限增长。
13. **Ollama 返回空/部分结果不回退 Gemini** — `app.py:893-902`。回退只判 `is None`，空 `{}`/截断 → 静默全英文字幕。
14. **Gemini 返回对象包裹 JSON → 全英文或 `TypeError`** — `app.py:924-932`。无 response schema；`{"subtitles":[...]}` 时推导迭代的是 dict 键。
15. **Gemini 回退合并循环 `KeyError`** — `app.py:1082-1099`。过滤用 `.get()` 但合并里 `sub['translation']`/`sub['start']`，缺字段整任务失败。
16. **dub/burn/slide 状态 SSE 直接 `json.dumps` live task dict** — `app.py:2450/2487/2534`，同 #5 竞争。**修:** `dict(...)` 快照。
17. **字幕 burn 吞掉 libx264 失败 → 返回过期/不存在的 `_burned.mp4`** — `app.py:2374-2388`（重试 guard `vcodec != "libx264"`）；`burn_dub_task:1845` 同模式。
18. **无视频流/非媒体上传致 `ffprobe` `IndexError`/`JSONDecodeError` → 未处理 500** — `app.py:2352-2360`(`streams[0]`，空 stdout)。
19. **`resegment_by_sentence` 返回空时 `dubbing_task` 崩** — `app.py:1562-1602`。非空 guard 查的是 reseg 前的列表；全空白译文 → gemini `subtitles[0]` IndexError 或空 concat 失败。
20. **同 `job_id` 并发配音竞争共享 `audio_dir`/`_dubbed.mp4`** — `app.py:1572-1578`。B 的 `shutil.rmtree` 删掉 A 正在写的文件，无在途保护。
21. **MTV `music_duration==0`(ffprobe 失败) → 空片段 → 含糊的 "片段合并失败"** — `mtv_processor.py:51-60/216-246`。**修:** 校验 `>0` 给明确错误。
22. **MTV librosa 运行期错误绕过 numpy/120BPM 回退** — `mtv_processor.py:82` 只捕 `ImportError`；解码/numba 失败直接整任务挂。
23. **`seconds_to_ass` 产生非法 `…:60.00` 时间码** — `app.py:216-217`。`sec=s%60` 经 `{:05.2f}` 把 59.999 进位成 60.00 而不进分钟。*(已核实)*
24. **auth token 允许走 URL query → 泄漏进代理/访问日志** — `app.py:2191`（公网经 Synology 反代暴露）。**修:** 只用 header/cookie。
25. **Slide-dub SSE `onerror` 在成功后误报红色失败** — `index.html:2162-2168` 缺 burn-dub 流程用的 `doneOk` guard。
26. **字幕/配音状态 SSE 断线不重连 → 前端卡进度** — `index.html:1408-1419`,`2243-2252` 仅 `close()`；MTV 有自动重连，这两个没有。

---

## LOW
- Gemini Files API 上传在 delete 前任何报错处泄漏（无 finally）— `app.py:1005-1126`
- Gemini 字符串 id(`"1"`) 与整型查找不匹配 → 整批静默英文 — `app.py:932-940`
- `parse_subtitles_json` 正则兜底截断含转义引号的译文 — `app.py:359-370`
- 上传文件名出现 `.` 后跟 `/` → ext 含斜杠 → `file.save` 500（非穿越，纯崩）— `app.py:2247`
- token 比较非常量时间（用 `hmac.compare_digest`）— `app.py:2192/2212`
- auth cookie 缺 `Secure` 标志（HTTPS 反代部署）— `app.py:2198/2214`
- `start.sh` 环境 sourcing 不剥 CR — `.env` 若从 Windows 重拷会潜在污染值 — `start.sh:7-11`
- MTV beat 吸附可能丢 slot → 片段变少+覆盖空隙，视频比音乐短 — `mtv_processor.py:216-246`
- `detect_gemini_tts_speed` 用错(默认) TTS 模型校准 + 浪费一次付费调用 — `app.py:1284-1299`
- 独立 slide-dub「需选文件」校验是死代码 → 含糊 "失败：no file" — `index.html:2069-2102`
- 每次 `onComplete()` 重复给 `dubVoice` 叠加匿名 change 监听器 — `index.html:1545-1553`
